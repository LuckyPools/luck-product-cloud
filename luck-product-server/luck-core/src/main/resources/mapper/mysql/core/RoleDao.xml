<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luck.cloud.core.dao.RoleDao">
    <select id="queryList" resultType="com.luck.cloud.core.entity.Role">
        select t.* from sys_role t
        <where>
            t.is_delete = 0
            <if test="id != null and id != ''">
              and t.id = #{id}
            </if>
            <if test="id != null and id != ''">
                and t.id = #{id}
            </if>
        </where>
        order by t.create_date desc
    </select>

    <select id="queryRolesByUserId" resultType="com.luck.cloud.core.entity.Role">
        select
            r.*
        from sys_role r
        inner join sys_role_user ru
            on r.id = ru.role_id
            and ru.user_id = #{userId}
            and ru.is_delete = 0
        where r.is_delete = 0
    </select>
</mapper>

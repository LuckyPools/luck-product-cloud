<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luck.cloud.core.dao.DictTypeDao">

    <resultMap id="dictTypeMap" type="com.luck.cloud.core.entity.DictType" autoMapping="true">
    </resultMap>

    <select id="queryList" resultMap="dictTypeMap">
        select t.* from sys_dict_type t
        <where>
            t.is_delete = 0
            <if test="id != null and id != ''">
              and t.id = #{id}
            </if>
        </where>
        order by t.create_date desc
    </select>
</mapper>
